{{- /*
  Rollout (étape 8) : stratégie blueGreen. Les deux versions tournent à
  pleine capacité en parallèle ; la bascule du trafic utilisateur
  (activeService) est un événement unique, pas une progression par poids.
  previewService permet de valider la nouvelle version avant bascule
  (curl planning-preview.devhub.local). scaleDownDelaySeconds garde
  l'ancienne version en vie 5 min après bascule pour un rollback instantané.
*/ -}}
{{- if not .Values.useDeployment }}
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "planning.fullname" . }}
  labels:
    {{- include "planning.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      {{- include "planning.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "planning.labels" . | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
        runAsUser: {{ .Values.securityContext.runAsUser }}
      containers:
        - name: planning
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.service.targetPort | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.env.LOG_LEVEL | quote }}
            - name: METRICS_BUCKETS
              value: {{ .Values.metrics.buckets | quote }}
            - name: METRICS_BUSINESS_ENABLED
              value: {{ .Values.metrics.businessEnabled | quote }}
            - name: VERSION
              value: {{ .Chart.AppVersion | quote }}
            - name: COMMIT
              value: {{ .Values.image.tag | quote }}
            - name: ROLLOUT_MARKER
              value: {{ .Values.rollout.marker | quote }}
            - name: FAIL_RATE
              value: {{ .Values.rollout.failRate | quote }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            capabilities:
              {{- toYaml .Values.securityContext.capabilities | nindent 14 }}
  strategy:
    blueGreen:
      activeService: {{ include "planning.fullname" . }}
      previewService: {{ include "planning.fullname" . }}-preview
      autoPromotionEnabled: {{ .Values.rollout.autoPromotionEnabled }}
      scaleDownDelaySeconds: 300
      {{- if .Values.rollout.prePromotionAnalysis }}
      prePromotionAnalysis:
        templates:
          - templateName: {{ include "planning.fullname" . }}-preview-check
        args:
          - name: preview-hash
            valueFrom:
              podTemplateHashValue: Latest
      {{- end }}
{{- end }}
